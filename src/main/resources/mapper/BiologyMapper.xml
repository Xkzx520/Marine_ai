<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.springboot.marine_ai.mapper.BiologyMapper">
    <select id="selectAll" resultType="com.springboot.marine_ai.entity.Biology">
        SELECT id, bio_name AS bioName, en_name AS enName, intro, 
               img_url AS imgUrl, create_time AS createTime
        FROM t_biology
        ORDER BY id
    </select>
    <select id="selectByName" resultType="com.springboot.marine_ai.entity.Biology">
        SELECT id, bio_name AS bioName, en_name AS enName, intro, 
               img_url AS imgUrl, create_time AS createTime
        FROM t_biology
        WHERE bio_name = #{bioName}
    </select>
</mapper>