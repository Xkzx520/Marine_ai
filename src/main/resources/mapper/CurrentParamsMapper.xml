<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.springboot.marine_ai.mapper.CurrentParamsMapper">
    <select id="selectOne" resultType="com.springboot.marine_ai.entity.CurrentParams">
        SELECT id, viscosity, diffusion, coriolis_intensity AS coriolisIntensity,
               max_wind_power AS maxWindPower, particle_max_count AS particleMaxCount,
               update_time AS updateTime
        FROM t_current_params
        LIMIT 1
    </select>
</mapper>